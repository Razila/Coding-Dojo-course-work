#value in the linked list as well as its position relative to the next value
class SLNode:
 def __init__(self, value):
  self.value = value
  self.next = None

#list of nodes, including the starting position (can use methods to define which
# value takes the starting position based on the head attribute

class SList:
 def __init__(self):
  self.head = None


